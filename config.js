const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "root",
    password: "fokou2014",
    database: "api_doc_db",
  },
  listPerPage: 10,
};

module.exports = config;
